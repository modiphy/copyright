export default {
  /**
   * Should be any acceptable CSS color value. Hex, RGB, RGBA, HSL, HSLA, etc.
   * default is 'inherit' which will inherit the color of the parent element.
   *
   * @type {String}
   */
  color: 'inherit',
  /**
   * Legal company name for copyright text.
   *
   * @type {String}
   */
  companyName: 'Company Name, LLC',
  /**
   * Should be any acceptable CSS color value. Hex, RGB, RGBA, HSL, HSLA, etc.
   * default is 'currentColor' which will inherit the color of the parent element.
   *
   * @type {String}
   */
  logoColor: 'currentColor',
  /**
   * Should be any acceptable CSS color value. Hex, RGB, RGBA, HSL, HSLA, etc.
   * default is 'inherit' which will inherit the color of the parent element.
   *
   * @type {String}
   */
  pipeColor: 'inherit',
  /**
   * Should be any acceptable CSS opacity value.
   *
   * @type {Number}
   */
  pipeOpacity: 0.5,
  /**
   * Should be any acceptable CSS margin value.
   *
   * @type {String}
   */
  pipeMargin: '0 0.5em',
}
